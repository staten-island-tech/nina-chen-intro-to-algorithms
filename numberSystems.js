// Binary to decimal
// Have the user input a string value, in binary
// Use a for loop to iterate through each value in the string until it reaches the last letter
// Everytime there is a one in the string,

// Decimal to binary
// Have the user input a number (decimal)
// Continuously multiply the decimal part of the number by two until the remainder is 0. (use while loop)
// For each time the whole number part of the number is 1, add a 1 to the answer (string)
// For each time the whole number part of the number is 0, add a 0 to the answer (string)

function decimalToBinary(decimal){
    let answer = "";
    while (remainder != 0){
        decimal === decimal*2;
        if (decimal)

    }
}